rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Single-user app — allow all reads/writes
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
